<?xml version="1.0" encoding="utf-8"?>
<!-- Source: res/drawable/widget_block_forest.xml — wide B's numeral block on an ordinary day. The layout is forced RTL, so the block sits on the right and only its right corners are rounded. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/widget_forest" />
    <corners android:topRightRadius="@dimen/widget_radius" android:bottomRightRadius="@dimen/widget_radius" />
</shape>
