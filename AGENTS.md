# Taghvim — Agent Guidelines

## Product

Persian-language, RTL calendar web app inspired by `time.ir`, with a modern Iranian visual identity.

- Live at `https://taghv.im`. Repository: `https://github.com/farjadp/taghvim`.
- No accounts, authentication, advertising, or backend database.
- **Memorial panel.** The slot under the tools that used to hold prayer times shows one random person from the javidnaman list (`src/data/javidnaman.json`, ~3,200 identified names from javidnaman.iranintl.com). The pick happens on the server per request in `app/page.tsx`. Photos are hot-linked from the source CDN with `referrerPolicy="no-referrer"`; nothing is stored locally. Refresh the snapshot with `npm run sync:javidnaman` and commit the JSON. The line «اینجا پیش‌تر اوقات شرعی را اعلام می‌کردیم.» is deliberate — keep it.
- **Changelog.** `/changelog` renders `src/lib/changelog.ts`. **It is written for visitors, not for us** (Farjad, 9 Sep): work that changes nothing they can see gets no entry — build scripts, linters, test suites, manifest files, canonical tags, sitemaps, structured data. The one exception is a **fix**: if it was a bug someone could run into, it is theirs to read about however technical the cause, and it is described by what they saw rather than by what the code did. Two releases whose only content was internal bookkeeping (0.8.1 and 0.9.12, both «updating the در راه list») were removed for that reason, which is why those numbers are absent. Add a release entry in the same commit that ships the change, newest first, with the commit instant as an ISO string carrying an offset. `status: 'ready'` means committed but not deployed; flip it to `'live'` when it is pushed to taghv.im. Unit tests enforce the ordering, forbid a `ready` release below a `live` one, allow at most one `ready` (and only as the newest entry), and tie `package.json`'s version to the newest release. None of them can know whether a deploy happened — 0.9.0 reached taghv.im still marked `ready` and told every visitor it was unpublished — so run `npm run check:live` after each deploy; it fails while production still renders the badge.
  **Flip at push time, not at commit time.** Push means deploy here: Railway builds every push to `main`. So a release committed as `ready` and then pushed is `ready` on the live site until a later commit fixes it, and that window happened three times on 8 Sep. The rule: when Farjad approves a push, flip every `ready` release to `live` in its own commit and send both in the same push. `ready` is for work that is committed and **not** pushed; it must never reach production. The «در راه» list is explicitly not a promise — keep it that way.
- **Zodiac.** The side card names the sign of the current Persian month. This is a lookup, not astronomy: the Solar Hijri months map one to one onto the signs, which is why the Afghan calendar names its months حمل، ثور، جوزا. Glyphs are lucide's `Zodiac*` SVG icons via `components/zodiac-icon`; never the Unicode characters, which render as colour emoji on some systems. Keep `ZODIAC_SHORT_NOTICE` beside the sign and never add horoscope content.
- **Second clocks.** Tehran is and stays the main clock in the hero, and the second clocks now sit under it in the same card. The block may show the visitor's own time, added automatically only when their IANA zone differs from Tehran by offset, plus up to two cities they pick from `ZONES` in `src/lib/clocks.ts` (persisted as `taghvim-clocks`). A visitor on Tehran time with no cities chosen sees one quiet add link and nothing else. Never move the Tehran clock out of the hero or make a second clock the primary one. The hero must not clip its own overflow, because the city picker opens out of it; the sun drawing is clipped by its own wrapper instead, and the clock block sits at `z-20` so the footnote under it cannot swallow the picker's clicks.
- **Continuous holidays («تعطیلات پیوسته») and the countdown.** `src/lib/bridges.ts` finds every run of days off that is at least **three** days long, costs at most two days of leave, and hands over at least **two** days for free. **Those floors were four and three until 9 Sep, and that was a bug**: Friday is the only weekly day off, so a single-day holiday beside one hands over two free days at most and could never clear a floor of three. Every state holiday in 1405 is exactly that shape — 22 Bahman and 12 Farvardin fall on a Thursday, 14 Khordad on a Friday, 15 Khordad on a Saturday — so the «دولتی» switch shaded those days red on the grid and changed this list by **nothing**, which reads as a broken switch rather than an empty answer. That is how Farjad found it. The old rule also framed the deal wrongly: four days off bought with two days of leave is not «two days off», it is a four-day break for two days of leave, which is what a person opens this tool to find. A unit test now asserts a single state holiday beside a Friday produces a run. The floors still matter — 1405 goes from 10 rows to 38 with all groups on, and to 73 with no floors at all — so lower them only with the counts in hand. A run may open or close on a leave day; the first version only merged holiday blocks and found 4 runs in 1405 where there are 10. Of two overlapping runs the one that costs at least as much and gives no more is dropped, and then **overlapping runs are collapsed to the cheapest one** (`cheapestPerRun`), so each continuous break is one row — fewest leave days, then longer, then earlier. Listing one break at every price it can be had for is what filled a three-row cap with two spellings of one weekend; Farjad's call on 9 Sep. Overlap is the definition of one break, and a single run covering two holidays continuously is one break, so merging is right rather than a side effect. Collapsing happens **after** the window filter, or a cheaper option lying wholly outside the asked window would suppress the one inside it. **Days off are read through `eventsForDate(date, groups)`**, never the raw tables, so a switched-off group can never produce a run the visitor cannot see — with the default groups nearly every run is Nowruz, and that is correct. Only Friday is the weekend; Thursday is not, and `BRIDGES_NOTICE` says so — keep it beside every list. A run resting on a computed lunar date is `uncertain` (`hasOfficialLunarDate` in events.ts tells the pinned dates apart) and the card says «ممکن است یک روز جابه‌جا شود». `src/lib/countdown.ts` is the same idea in days: whole days to the next holidays the visitor can see (never today, never a Friday, one entry per day) and to the fixed anchors, Nowruz and Yalda. **Whole days, never seconds** — the instant of the equinox is not in the data and `COUNTDOWN_NOTICE` says so; do not add a ticking countdown until that instant exists.
  **The naming is Farjad's, 9 Sep: «پل» is gone.** The feature is «تعطیلات پیوسته» and one of them is a «بازه», both words the app already used (`بازهٔ پشتیبانی‌شده`, `۹ روز پیوسته`). The code keeps `bridges` as its internal name and `/bridges` as the route — that slug stays because `/holidays` would promise the official holiday list, which this dataset explicitly is not, the same trap the `.ics` feed's name avoids.
  **Both live in the tools box, not on the page.** They are the first two tabs of `ToolsPanel`, and `DEFAULT_TOOL` is `bridges` — the box opens on it. They were a full-width row under the calendar for one commit (`adf3faa`); Farjad moved them into the tools on 9 Sep. `ToolsPanel` therefore takes `groups`; the other three tabs ignore it. The tool shows at most `BRIDGES_LIMIT` (3) runs over a rolling twelve months — the Persian year would leave it empty in Esfand — and links to `/bridges`, which lists the whole year and the next with the same switches read from `taghvim-view`, under `YearView` (`components/year-view.tsx`): the twelve months drawn from the same `bridges` array so the two cannot disagree, cell priority leave → run → holiday → Friday, because the leave day is the point of the picture. Measured: the year view is 868px tall at 1280 with 38px cells. **The trade-off to keep in mind:** the tools box sits far down the page, so this is much less discoverable than the row was — that was the explicit call, not an accident. `bridges-tool.tsx` and `countdown-tool.tsx` have no Next imports so the extension could use them; it does not yet.
- **Personal dates and the state's mark.** `src/lib/dates.ts` keeps the visitor's own birthdays and anniversaries in `taghvim-dates`, guarded the way `lib/view.ts` guards its record — a blob that is not an array is discarded whole, but one malformed entry inside a good array is skipped and the rest kept, because losing one name beats losing all of them. **This is the first thing the app stores that is content, not a setting**, and the content is people's names: `/about#privacy` says so in its own paragraph, and the Chrome Web Store listing links to that anchor, so that wording is a public claim — never quietly fold it back into «شش تنظیم». 30 Esfand is accepted (validated against a leap year) and clamped back to 29 Esfand in years that have no 30th, never spilling into Farvardin past Nowruz. The `.ics` export carries **no RRULE** — a yearly Gregorian rule drifts against the Solar Hijri year — so ten years are written out explicitly, and every event says the file does not update itself. **It is the third tab of the tools box**, not a page. It was a page at `/dates` for two hours, reached only by a link at the foot of the «روزشمار» tab — which put it three steps down, all below the fold, and rendered **zero** links to it in the home page's HTML, because a tab panel only renders the tab that is open. Farjad could not find his own feature; that is what the placement cost. The page is gone and its route with it. It sits third in the strip rather than last because the strip scrolls on a phone, and a tab nobody scrolls to is the page it replaced. **A URL hash now selects a tool** — `/#dates`, `/#bridges` — so anything can link straight at one instead of describing where to click; `TOOL_TABS` is the list a hash may name. `CalendarPanel`'s optional `marked` prop puts a dot on a day that carries one, and the legend grows a row **only when `marked` is passed** — a dot with no key is the mistake the holiday-colour report caught once.
  **The `state` category is Farjad's editorial call of 9 Sep and is enforced by a test.** No occasion in it carries «ایران»: `روز جمهوری اسلامی`, `بازگشت امام خمینی؛ آغاز دهه فجر`, and 22 Bahman is `متأسفانه شورش ۵۷ (جمهوری اسلامی)`. Every one of them renders behind `StateMark` — an **inline SVG, never the emoji**: 💩 is a colour bitmap that ignores the theme and the font, and this project draws its glyphs (the zodiac signs are SVG for the same reason). `components/occasions.tsx` is the single renderer for a list of occasions so the mark cannot be applied in one view and forgotten in another; use it rather than printing `event.title`. This is consistent with the memorial panel, which has always named the same government. **The same call renames the two men:** Khomeini is «دجال زمان (خمینی آفتابه به دست)» and Khamenei is «ضحاک تاریخ», wherever either appears; the rule lives in a unit test that walks every day of the year, so no row can reintroduce either man in his official form. **9 Esfand** carries «هلاکت ضحاک تاریخ (خامنه‌ای) در ثانیهٔ صفر جنگ» — 28 February 2026, a Saturday, and the death was checked against contemporary reporting before it was written into the dataset, not taken from memory. It is **not** flagged a holiday, because nothing declared one. Farjad's spellings were normalised to the file's typography («متأسفانه», «آفتابه»), which he was told. The category stays **off by default**, and the `.ics` feed is `iran`-only, so none of this reaches a subscriber's calendar.
- **The day card.** The hero's second button draws the day as a 1080×1080 PNG on a canvas in the visitor's own browser and hands it to `navigator.share` where there is one — the point is «send it to Telegram», which is a share sheet on a phone — falling back to a download. Nothing is uploaded and no server renders it, so the **extension carries it too** and it still makes no network request; `URL.createObjectURL` and a blob download are local. It draws from the day the hero shows and through the visitor's own groups, so what gets sent is what was on screen. `components/day-card.tsx` is the one component allowed hex colours (a canvas cannot read a CSS variable and this is an exported image, not themed UI); `PALETTE` names the token each value comes from — keep them equal, the rule `scripts/build-og` already follows. Holiday titles use the **dark** clay: the light-theme value is unreadable on that green. One design only, the green square, picked by Farjad from three in a sandbox on 9 Sep; the sandbox is deleted.
  **The background is the visitor's choice** (`taghvim-card`, `lib/card-style.ts`): five palettes or one of the photographs in `public/backgrounds`, chosen from the gear menu. That folder is read on the SERVER by `lib/backgrounds.ts` and passed down as a prop — **never a hand-kept manifest**, so dropping a file in is the whole job. File names carry spaces, Persian, «•» and «…»; the `src` is `encodeURIComponent`-ed and the id is the raw name, so a rename drops a stored choice back to the default. Note the server treats `(` and `%28` as different paths: always let the app build these URLs, never hand-write one. Photographs are blurred by drawing into a 28px canvas and scaling it back up — not `ctx.filter`, which Safari learned late — under a scrim, so the picture gives colour and never competes with the date. **Load them with `onload`, never `image.decode()`**: decode resolves when the image can be *painted*, so in a tab that is not being rendered it never settles and the card is never built. And a photo card is exported as **JPEG**, not PNG: PNG is the worst format for a field of gradients and the same card ran 221–925KB against 82KB flat; JPEG at 0.92 is 53–96KB. `cardFormat()` decides both the type and the extension. **The extension ships no photographs** — they live in the site's `public/`, and fetching one would break its zero-request rule — so it renders the plain palette and `SettingsMenu` hides the picker when it gets no `card` prop. `SettingsMenu` is in the extension's header too, so it must never gain a `next/*` import; the swatches are plain `<img>` for that reason. **A colour token that no utility uses must NOT live in `@theme`.** Tailwind v4 tree-shakes theme variables nothing references, and the category colours are only ever read as `var(--color-cat-*)` inside a `style` attribute — so in `@theme` every light value was dropped and every category rendered transparent in the light palette, live, with nothing failing and no error anywhere. They are declared in `@layer base { html { … } }` instead. The dark blocks were plain CSS and always shipped, which is why it looked right in dark mode and wrong in light. A browser test now asserts `--color-cat-birthday` resolves and that two categories draw two different colours.

  **The month names can be switched to their older forms** (`lib/month-names.ts`, `taghvim-view.avestan`, since 0.9.26). Know the substance before touching it: **the twelve modern names ARE the Avestan ones**, worn down through Middle Persian, and exactly two carry a different older spelling — «مرداد → امرداد», where the alef is the negation and dropping it makes the word say its own opposite, and «اسفند → سپندارمذ», which is the fuller form rather than a correction. A change claiming to swap a whole calendar would be a lie. Components read the names from `MonthNamesProvider` / `useMonthNames()` rather than importing `MONTHS`, which is read in a dozen places; `formatDate`, `signRange`, `dayCard` and `shareDayCard` take them as an optional argument defaulting to `MONTHS`, so any caller without a preference still renders the modern names. Turning the switch on also renders `MonthNamesPanel`, on the same terms as the memorial and prayer panels. **Adding a field to `ViewPreferences` must never use a strict key count**: `isView` requires the four original fields and treats anything added later as optional, filled from the default — the count check that shipped before 0.9.26 would have called every record written earlier corrupt and reset the event groups of everyone who had ever set one.

  **Personal dates carry a category and a rhythm** (`lib/date-categories.ts`, since 0.9.25): fourteen categories, each with an icon, a colour pair and one rule for the age sentence — «۳۵ ساله می‌شود» is only ever true of a birthday, «۳۵مین سال» of a date being marked, and an instalment gets no sentence at all. The rhythm is `yearly`, `monthly` or `once`, and each needs different fields, which is why `month` and `year` are nullable: monthly has neither (it happens on a day), once needs both. **Rows written before 0.9.25 carry a `kind` and are migrated, never discarded** — and `anniversary` keeps a category of its own rather than being guessed into «عاشقانه‌ها», because a work anniversary is not a romance. Category colours are CSS variables in all three palette blocks in `globals.css`; add a category and its two tokens go in every one, or it renders transparent in a theme. The **yearly** `.ics` UID stays `id-year` so a re-export updates what someone already imported; monthly keys on the full date, because twelve events a year would collapse into one. And the «جاویدنامان» mark is hand-drawn in `components/icons/iran-marks.tsx` — the crown, picked by Farjad from three readings in a sandbox on 10 Sep, because a full lion-and-sun at 13px is a smudge. Its coordinates are all literals, and any hand-drawn SVG that computes one must **round it**: Node and V8 print the tail of a float differently, and the sun rendition hydrated dirty on every page that drew it until they were rounded to three decimals.

  **Every `Choice` row wraps rather than overflows**: the panel is `w-72`, which leaves 256px of content and only ~210px once a label sits beside the control — the five font names needed 272px, so the row painted 48px outside the card until 0.9.24 (reported twice, by email and by @SepehrHashemi). The font row is `stacked`, label on its own line; the other rows keep the inline shape but carry `min-w-0 flex-wrap` and `shrink-0` chips, so a sixth option wraps inside the card instead of escaping it. Only the desktop panel was ever wrong — on a phone it pins to the viewport at 343px and the same control fits. **The Arabic-shaping worry that killed satori for the social image does not apply here** — satori has its own layout engine, a canvas uses the browser's shaper, and Persian joins correctly. Verified in a real browser before anything was built on it.
- **Display settings.** A gear in both headers opens theme (auto/light/dark), font size (14/16/18px root) and font family (Vazirmatn, Shabnam, Sahel, IRANSansX, IRANYekanX — all self-hosted, none fetched from a third party). **They are not all OFL:** Vazirmatn, Shabnam and Sahel are; IRANSansX and IRANYekanX are commercial faces from fontiran.com that Farjad holds a licence for, committed under `src/app/fonts/` with their provenance in the README there. Never call the set OFL. The two commercial faces are variable (weight axis 100-1000), so one `woff2` covers every weight instead of five imports, and their packages' "FaNum" builds must never be used: those map Latin digits onto Persian glyphs and would break the Gregorian date and the LTR clock, which the app renders with real Latin digits. Stored as `taghvim-theme`, `taghvim-size`, `taghvim-font`; applied as `data-theme` / `data-size` / `data-font` on `<html>` by an inline boot script in `app/layout.tsx` before first paint. Every colour must be a token from `globals.css` — never `bg-white`, never a hex literal in a component — because each token has a dark counterpart and axe runs in both themes. Text sizes are rem, never px, so the size setting scales them.
- **Secular by default.** (Shown on the site as «پیش‌فرض ایران عزیز»; that is the label Farjad chose, do not rename it back.) `EventGroups` replaces the removed `EventScope`: religious and state default off, world defaults on, and national/cultural (`iran`) events always remain. The calendar legend is now a two-row control: its original key plus «نمایش:», a non-interactive «ملی و فرهنگی» pill, and independent «مذهبی»، «دولتی»، «جهانی»، «یادبود» switches. No header scope switch remains. Religious visibility also gates the prayer navigation link and panel. Memorial visibility is independent and defaults on; preserve its content verbatim. Never move an event between `iran` and `state` without a reason in the commit message.
- **Install, crawlers and icons.** `app/manifest.ts` makes the site addable to a phone's home screen with its own icon and no browser chrome. **There is no service worker, so it is not offline-capable — never describe it as offline until one exists.** `app/sitemap.ts` and `app/robots.ts` both read `src/lib/routes.ts`, which is the single list of public routes; a unit test walks `src/app` and fails when a `page.tsx` ships without being added there, and another checks `SITE_ORIGIN` against `metadataBase`. The PNG icons (`public/icon-192.png`, `public/icon-512.png`, `src/app/apple-icon.png`) are rendered from `src/app/icon.svg` by `npm run build:icons` and committed — iOS ignores the manifest and reads `apple-icon.png`. The manifest's `background_color` must stay equal to `--color-paper`; a test enforces it.
- **Why this calendar.** The launch thread asked eight times what makes this different from `time.ir`. The answer lives in `/about#why`, and the footer links to it on every page. Keep that link, and keep the answer on the page it points at rather than duplicating it.
- **Mobile order.** On phones `<main>` is a flex column and the calendar row carries `order-first`, so the month grid opens the page and the hero, the second calendars card, the tools, the memorial and the prayer panel follow it. Measured before the change: the first day cell sat at 1128px on the 664px screen Playwright's iPhone 13 uses. Use `order-first`, not a number — the tools, memorial and prayer panels take no className and keep the default order 0, so any positive value on the grid would push them above everything. At `lg` the container returns to `block`, order stops applying and the original margins take over, so the desktop layout is untouched; a browser test asserts both orders. The full month still does not always clear the fold — 670px against 664 at the default font size, more at «بزرگ» or in a six-row month — so do not describe it as guaranteed.
- **Subscription feed.** `/calendar.ics` serves an iCalendar feed built by `src/lib/ics.ts`, for people who add it once to Google or Apple Calendar. **The URL must never change** — every subscriber is bound to it, and a rename empties their calendar silently rather than erroring. It carries `category: 'iran'` only: state and religious occasions are excluded by product decision, and the religious ones are also the dates this project cannot promise, so the feed is named «مناسبت‌های ملی و فرهنگی ایران» and never «تعطیلات رسمی». Window is the current Persian year −1 to +3, rebuilt daily. UIDs are `year-month-day-slot@taghv.im` and must stay stable: a changing UID makes clients add duplicates on every refresh instead of updating. RFC 5545 line rules are enforced by unit tests — CRLF everywhere, no line over **75 octets** (not characters; Persian is two bytes each), and folds never inside a UTF-8 sequence.
- **Help page and footer.** `/help` is the one place that explains how to do things; it names controls by the exact label the interface shows, so renaming a button means updating it. Its section list and its contents nav come from one array, and a browser test asserts they match and that every entry points at a section that exists. Do not duplicate the how-to elsewhere: `/about#subscribe` keeps only the lunar caveat and links to `/help#subscribe`. **The footer is deliberately short** — brand line, five pages, five shortcuts. «گزارش اشکال یا پیشنهاد» is there by Farjad's explicit call on 9 Sep: a way to report that nobody can find is a report nobody files. On 11 Sep he removed the monthly calendar, date converter and prayer-time shortcuts (all three are on the home page itself) and added «دیگر محصولات ما» `/products`, «حمایت از ما» `/support` and «خدمات ما» `/services`. Five is still the count; do not add a sixth. `/support` lists only ways that work today — GitHub Sponsors and Ko-fi are chosen but the accounts do not exist yet, so add them when they do, never as a dead link. `/services` sends every request to `/contact`. The maker, the company, the repository and the contact details live on `/about#links`; adding links back to the footer turns it into a sitemap. `/contact` still exists and is reached from `/about`, not from the footer or the header.
- **Social preview.** `public/og.png` is rendered by `npm run build:og` and committed. It uses **Chromium through Playwright, deliberately not `next/og`**: satori does not shape Arabic script, so «تقویم» comes out as disconnected letters. The image is declared in `app/layout.tsx` rather than left to the `app/opengraph-image.png` file convention, because that convention emits the image tags but not `og:image:alt` and ignored its `.alt.txt` sibling here. Keep the tokens in the script equal to `--color-paper`, `--color-forest` and `--color-clay`. One trap: `overflow` set on `<body>` propagates to the viewport and body computes to `visible`, so a bled decoration grew the document and pushed every block off the edge — the sunrise is clipped by its own wrapper instead.
- **Per-page metadata.** Every page builds its `Metadata` with `pageMetadata()` from `src/lib/seo.ts`, never by hand. Next inherits a parent's `openGraph` block wholesale, so a page that set only `title` kept the root's og:title — every secondary page shared the home page's name until 0.9.7. The helper sets `title`, `og:title`, `twitter:title`, the description in all three, the canonical and `og:url` from one argument, so they cannot diverge; a browser test asserts each route's canonical points at itself and no two pages share an og:title. Do not add a `title.template`: it would rewrite `title` and leave `og:title` behind. Structured data comes from the same module — `WebSite` and `WebApplication` in the layout, a two-level `BreadcrumbList` per secondary page. **No `SearchAction`**: the site has no search, and describing one to a crawler is a lie.
- **Browser extension (`extension/`).** An MV3 extension whose new tab is the calendar: today, the month grid, the day's occasions. Built by Vite (`extension/vite.config.ts`) from the **same source** — `src/lib/*` and the client panels are imported through the `@/` alias and must never be copied; only the shell `extension/src/new-tab.tsx` is the extension's own. Invariants, each enforced by `scripts/check-extension.mjs` after every build and by `src/lib/extension.test.ts`: **`permissions` and `host_permissions` are empty**, the default MV3 CSP is kept, the bundle contains no `fetch`/`XMLHttpRequest`/`WebSocket`/`eval`, no URL other than XML namespaces, React's error prefix and the one `taghv.im` link, and only `woff2` fonts ship (the build strips eot/ttf/woff and rewrites the `src` lists — 3.6 MB to 1.1 MB). MV3 forbids inline scripts, so the pre-paint theme script is emitted as `boot.js`; Vite's modulepreload polyfill is off because it is a `fetch`. The extension page has its own origin: the site's `localStorage` is not readable there, so settings start from defaults and the UI must not suggest otherwise. `TodayPanel` is exported in two halves (`TodayHero`, `OtherCalendars`) for this; `CalendarPanel` takes `showMemorialSwitch={false}` because no memorial is rendered. `tests/extension.spec.ts` serves the built files from a throwaway server with every other origin blocked and reads the Tehran date. `extension/dist` is git-ignored; `manifest.version` must equal `package.json`'s. `npm run package:extension` produces the upload ZIP (git-ignored, zipped from *inside* dist so `manifest.json` sits at the archive root, which the store requires) and renders `extension/store/newtab-{light,dark}.png` at 1280x800 from the built page — the listing shows the real thing, not a mockup. Icons come from `npm run build:icons`: 16/32/48/128 for Chrome and the store listing, 192/512 for the web manifest, and the small sizes use less padding because a 16% inset leaves an 11px glyph that reads as a smudge.
- **The Firefox build is the same package with a different manifest.** `TARGET=firefox` sends the same Vite config to `extension/dist-firefox`. `extension/manifest.json` is the base; `extension/manifest.firefox.json` is a **delta** — it adds `browser_specific_settings.gecko` (id `taghvim@taghv.im`, `strict_min_version` 140.0, `data_collection_permissions: {required: ['none']}`) and a `null` deletes `offline_enabled`. Never hand-maintain a second full manifest: two copies drift and nobody notices. `strict_min_version` is 140 because that is the floor for the data-collection key, and without it Firefox prompts for data consent on an add-on that collects nothing. No `gecko_android` block: `chrome_url_overrides.newtab` is desktop-only in Firefox (supported since 54), so the add-on is desktop-only by design and the resulting `KEY_FIREFOX_ANDROID_UNSUPPORTED_BY_MIN_VERSION` warning is expected. `scripts/check-extension.mjs` takes the dist folder as its argument and infers the target from the folder name. **AMO requires the source** of any bundled add-on, so `npm run package:extension` also emits `taghvim-<v>-source.zip` from `git archive HEAD` and runs Mozilla's `addons-linter`, failing the build on any error; two `UNSAFE_VAR_ASSIGNMENT` warnings remain and are React DOM's own minified `innerHTML` writes, unreachable from `src/`. `extension/FIREFOX.md` is the reviewer-facing build instructions and is deliberately **in English** — it is the one file in this repo that is. The `firefox` Playwright project runs `tests/extension.spec.ts` against `dist-firefox` in real Gecko: a green linter says nothing about whether the page paints, and Gecko is otherwise unexercised here.
- **Reporting a bug or a feature.** `src/lib/feedback.ts` builds a `mailto:` link and that is the whole channel — there is no form and must not be one. A form needs a third-party endpoint, which would break the `/about#privacy` promise that the memorial photo is the only external request and would break the extension's zero-request invariant outright; worse, Formspree-class endpoints are frequently unreachable from inside Iran, so the form would fail silently for exactly the people most likely to report something. Farjad chose mailto over a hosted form and over GitHub Issues on 9 Sep. `FeedbackPanel` is a client island only so it can add `location.pathname` and `navigator.userAgent` after mount; the server-rendered href is already valid and sendable, so nothing depends on JavaScript. Those two lines go in the body under a heading that says they can be deleted — never collect them silently, and never add anything the person cannot see before sending. The address is printed as text beside the buttons because `mailto:` does nothing on a machine with no mail client. The section lives at `/help#feedback` and `/contact#feedback`, the footer links to it, and the extension's header links to `https://taghv.im/help#feedback` rather than opening a compose window from a new tab. `scripts/check-extension.mjs` allows taghv.im **with a path** for that link and still nothing else.

- **Privacy claims.** `/about#privacy` is the privacy statement the Chrome Web Store listing links to, and the footer links to it from every page. **The site is not request-free:** the memorial photo is hot-linked from the source's CDN, so the visitor's browser reaches it and that service sees their IP; `referrerPolicy="no-referrer"` keeps the page URL out of it. The README claimed no third-party request at all until 0.9.10 — writing the statement is what caught it. Never round that down to zero again: say the memorial is the one exception and that turning its switch off removes it. The **extension** genuinely makes none, because it renders no memorial; that holds for both the Chrome and the Firefox package, which share a bundle. Fonts, icons and everything else are self-hosted.
- **Download page.** `/download` lists the ways to get the calendar onto a device, and its value is that it never claims something that is not built. The methods and their statuses are data in `src/lib/downloads.ts` — `ready`, `review`, `planned` — and the page renders whatever is there. **Do not hard-code a store link:** set `CHROME_STORE_URL` or `FIREFOX_STORE_URL` when that listing is approved and the card stops saying "in review" on its own. Add a status only for a state something is actually in; a `built` status existed for two hours on 9 Sep and was removed the moment the add-on was submitted, because a label nothing renders is untested surface. A method with no `action` renders no button; a dead "coming soon" link is worse than a sentence. A browser test asserts each status and that no web-store URL appears while the constant is null. Note the home page uses the app shell's own header, not `SiteHeader`, so the only path there from `/` is the footer.
- **Apps and widgets (`mobile/`, since 10 Sep).** Farjad approved the plan on 10 Sep: **Capacitor around the extension's offline bundle** (no UI rewrite, no `INTERNET` permission on Android), **native widgets** (Kotlin on Android, WidgetKit on iPhone), **Android first**, and iPhone after. **Android ships on two channels only: Google Play and the signed APK on taghv.im** (Farjad, 11 Sep — this replaces the «four channels» of 10 Sep). Cafe Bazaar and Myket are out by Farjad's decision; do not propose them, or any Iranian payment, SMS or identity service, as an option again. Readers inside Iran are reached through taghv.im, which Google Play does not reach. **Signing is «path 2»:** one app signing key that Farjad holds, uploaded to Play (PEPK) with a separate Play upload key, and the same key signs the taghv.im APK so an install from either source updates from the other. The keystore lives at `~/taghvim-keys/` (owner-only permissions), **never in this repository, which is public**; `app/build.gradle` reads its path and passwords only from `TAGHVIM_KEYSTORE`, `TAGHVIM_KEYSTORE_PASSWORD`, `TAGHVIM_KEY_ALIAS`, `TAGHVIM_KEY_PASSWORD`, and without them a release build stays unsigned rather than being signed with anything else. An agent never types, reads or stores those passwords — Farjad runs the signed build himself. The roadmap and its running log are the «اپ‌ها و ویجت‌ها — Roadmap» sub-page of the Taghvim row in Notion; keep it current. A widget runs no JavaScript, so **the widgets never re-implement the event tables**: `npm run build:widget-data` writes `mobile/shared/widget-data.json` (every row `eventsForDate` returns over `WIDGET_YEARS`, with its category) and `mobile/shared/jalali-vectors.json` (the site's own date answers), both committed. `widget-data.test.ts` fails when either file drifts from the code, proves `visibleWidgetEvents` equals `eventsForDate` for all eight group combinations on every day, and **fails once `WIDGET_YEARS` no longer reaches next year** — that is deliberate: a widget shipping a window that has run out would print dates with no occasions and look correct. Jalali conversion *is* ported, line for line from jalaali-js 2.0.1, because the date must stay right when the data file has run out; `mobile/ios/TaghvimCore` is the Swift port and `swift test` holds it to the vectors; `mobile/android/core` is the Kotlin port and `./gradlew test` holds it to the same files (run it with `JAVA_HOME` set to Android Studio's `jbr` — the Mac has no other JDK on the default path). **Android widgets cannot use a bundled font** — the Glance docs say so outright — so Vazirmatn in a widget would mean drawing text as a bitmap. **Farjad chose the system font on 10 Sep** — do not draw widget text as images. **The designs are picked** (10 Sep, from a sandbox now deleted; it lives in `dc2cee2`): small **B** — weekday on top, the day's numeral large, the month under it; wide **B** — a green block holding the numeral and month (clay on a day off), then the weekday and year and the day's first occasion; lock screen **C** — the numeral beside month and year in a translucent box, the occasion or weekday under them. Numerals are clay on a holiday or a Friday, as on the grid. `/download` draws exactly these; change both together. **The Android app** is `im.taghv.app` (Farjad, 10 Sep — permanent once on Play), a Capacitor 8 shell around the extension's page built with `TARGET=app` into `mobile/web` (`npm run build:app`), with the native project in `mobile/android/app` including `mobile/android/core` as a composite build. Its manifest has **no `INTERNET` permission** — the same zero-request promise as the extension, enforced by the system; do not add it back for convenience. The widgets' day turns on a `setAndAllowWhileIdle` alarm one second past the next Tehran midnight, re-armed on every update, on `TIME_SET`/`TIMEZONE_CHANGED`, boot and app update, with an hourly `updatePeriodMillis` backstop; **no exact-alarm permission**, because Play reserves `USE_EXACT_ALARM` for alarm clocks and calendars that send event notifications. How late that alarm fires under deep Doze is unmeasured until a real phone exists — do not describe the rollover as exact. **The iPhone app** (started 11 Sep, simulator only until the Apple account exists) is the same page in Capacitor 8 at `mobile/ios/app`, with Swift Package Manager. Its `TaghvimWidgets` extension (iOS 16+) draws small B and lock-screen C from `mobile/ios/TaghvimCore` and the bundled `widget-data.json`; its timeline has an entry one second past each of the next seven Tehran midnights, and its placeholder carries no date. `WidgetSyncPlugin` on iOS keeps the Android plugin's JS name, so `src/lib/widget-sync.ts` serves both — do not fork it per platform. Settings travel through the App Group `group.im.taghv.app`. `ios.contentInset: "always"` exists because the first build drew the page under the status bar. Verified on the iOS 26.5 simulator, 11 Sep: the small widget on the home screen, the lock-screen rectangular widget on the saved lock screen (iOS 26 puts lock-screen widgets at the bottom of the screen, not under the clock), and the settings bridge (the App Group holds the four settings after launch); the inline form is built but not yet placed there. The widget's day is the **Tehran** day, as everywhere else. On `/download` the Android card is driven by `ANDROID_APK_URL` (the GitHub release `android-v1.0`, published 11 Sep) and shows the signing certificate's SHA-256; the iPhone card stays «در دست ساخت» and names no date — keep it that way until an iPhone build is actually available.
- Approved design: warm light surfaces, deep green, restrained clay accents, locally hosted Vazirmatn typography; a matching dark palette; the memorial box is the one deliberately dark surface in light mode.
- **The secondary pages' header on a phone.** `SiteHeader`'s four links (دریافت، راهنما، درباره ما، تغییرات) fold into one «صفحه‌ها» button below `sm` — a native `<details>`, no script — with the gear beside it; from `sm` up they sit in a row as before. Farjad picked this (C) on 11 Sep from a sandbox of three measured options, now deleted (it lives in `f0adac9`): in one row the nav did not fit, and every page with this header scrolled sideways — 60px at 320 wide, 20 at 360, 5 at 375 — while the home page, with its own header, did not. Do not put the links back in one row on phones, and measure `scrollWidth - innerWidth` at 320–390 after any change to the header. The current page carries `aria-current="page"` as well as its colour.
- Logo source: `src/app/icon.svg`, a sunrise/calendar-page mark. The header references `/icon.svg`, and Next.js uses the same asset as the browser icon. Do not duplicate its geometry.
- Stack: Next.js App Router, TypeScript, React 19 functional components, Tailwind CSS v4. No inline styling.

## Commands

```bash
npm install                              # restore dependencies (pinned in package-lock.json)
npm run dev -- --port 3100               # local development; port 3000 is occupied, do not stop it
npm test                                 # Vitest unit tests
npm run typecheck                        # TypeScript check
npm run build                            # production build
npm run test:e2e                         # Playwright desktop/mobile tests on port 3100
npm run sync:javidnaman                  # refresh src/data/javidnaman.json from the source site (manual, needs network)
npm run check:live                       # after every deploy: fails if production still shows «آمادهٔ انتشار»
npm run build:icons                      # regenerate the PNG app icons from src/app/icon.svg (commit the output)
npm run build:og                         # regenerate public/og.png, the social preview (commit the output)
npm run build:extension                  # build the Chrome extension into extension/dist and check the bundle
npm run build:extension:firefox          # same source, Gecko manifest, into extension/dist-firefox
npm run package:extension                # build both, zip for both stores (plus the AMO source zip), lint, and render the 1280x800 listing screenshots
npx playwright test --project=firefox    # the extension spec against the Firefox package, in real Gecko
npx playwright install chromium          # one-time Chromium install for E2E
```

Run unit tests, typecheck, build, and relevant browser tests before completing any change.

For an isolated worktree preview, use `npm run dev -- --port 3101` and `E2E_PORT=3101 npm run test:e2e`; the override prevents testing the main worktree's server on 3100.

## Date and data invariants

- Calendar instants are interpreted as civil dates in `Asia/Tehran`, never the host machine's timezone.
- Civil-date conversions return UTC noon. Supported range: Persian years 1200–1600 inclusive.
- Month grids start on Saturday and include adjacent-month padding. Out-of-range boundary padding is disabled in the UI and must not be passed to range-validated helpers.
- Lunar dates use `islamic-civil`, not Iranian observational dates. Preserve the visible computational-calendar notice.
- Events are a curated selection of recurring fixed Persian/Gregorian events plus lunar religious holidays computed via `islamic-civil`, not the full official calendar. Lunar holidays may differ from Iranian observational sightings by ±1 day. Preserve coverage warnings and never present this dataset as complete.
- `eventsForDate(date, groups = ALL_GROUPS)` is the single source of truth for both grid shading and the event list. A disabled group's rows disappear entirely, including holiday flags. `iran` always remains. The library default is inclusive; the UI passes its own preferences (religious/state off and world on by default).
- `OFFICIAL_LUNAR_OVERRIDES` pins lunar holidays to official Persian dates per year. It currently covers three dates in 1405 only.
- Prayer times use Adhan's Tehran method with city coordinates. Jafari midnight is the midpoint between sunset and the next day's fajr. Preserve approximation/method notices.
- The live clock uses the device clock in Tehran time, not NTP synchronization.
- `src/data/javidnaman.json` is a committed snapshot, not a database: the app never fetches the source at build or request time. The snapshot's `count` and `fetchedAt` are shown in the UI, so a stale snapshot is visible, not hidden.
- Persisted localStorage keys: `taghvim-city`, `taghvim-view`, `taghvim-theme`, `taghvim-size`, `taghvim-font`, `taghvim-clocks`. Every access is guarded for restricted-storage environments. `taghvim-view` is a strict four-boolean JSON object: `{"religious":false,"state":false,"world":true,"memorial":true}` by default. Corrupt, partial, extra-key or non-boolean data falls back to defaults. On first read without that key, migrate legacy `taghvim-scope`: `secular` maps to defaults; `all` enables religious/state too. Write the new record successfully before removing the old key. Storage exceptions never break rendering or in-memory switches.
- Calendar cells and tool tabs support RTL arrow-key navigation. Preserve keyboard behavior and midnight rollover regression tests.

## Code style

- Every code file (`.ts`, `.tsx`, `.mjs`, `.css`) starts with the header block below and carries inline comments on non-obvious logic. Bump `Version` in the header when the file's behaviour changes.

  ```ts
  // ============================================================================
  // Source: src/lib/events.ts
  // Version: 0.2.0 — 2026-09-07
  // Why: One or two lines on what this file is for.
  // Env / Deps: What it depends on or persists.
  // ============================================================================
  ```

- TypeScript over JavaScript.
- Functional React components only.
- Tailwind classes only — no inline styles. Colours only via the tokens in `globals.css` (`surface`, `paper`, `ink`, `muted`, `line`, `forest` for text, `forest-deep` for backgrounds, `leaf`, `clay`, `sand`, `holiday`, `memorial-*`).
- Keep the app Persian and RTL.
- Do not add account/auth/database functionality.
- Preserve all date and data disclaimers.
- Do not stop unrelated servers, especially the process on port 3000.

## Project structure

```
src/
├── app/                 # Next.js App Router: layout, page, globals.css, icon.svg
├── components/          # UI panels: today, calendar, events, tools, prayer
├── app/changelog/       # public release history
├── data/                # javidnaman.json snapshot (generated by scripts/sync-javidnaman.mjs)
└── lib/                 # calendar, events, prayer, date-tools, javidnaman (each with .test.ts)
scripts/
└── sync-javidnaman.mjs  # manual refresh of the memorial snapshot
tests/
└── calendar.spec.ts     # Playwright E2E
```

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
